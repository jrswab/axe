getting this error in the github workflow:  docs/plans/ISS-59_m6_streaming_smoke_tests_spec.md

---

getting this error in the github workflow: Run OUTPUT=$(./axe run --agents-dir .github/smoke-agents cfg-params -p "Reply with only the word: pong")
  OUTPUT=$(./axe run --agents-dir .github/smoke-agents cfg-params -p "Reply with only the word: pong")
  echo "$OUTPUT"
  [ -n "$OUTPUT" ] || { echo "FAIL: empty output"; exit 1; }
  shell: /usr/bin/bash -e {0}
  env:
    OPENCODE_API_KEY: ***

FAIL: empty output
Error: Process completed with exit code 1.

---

I ran the flow again and it failed again. what does this smoke test actually test?

---

minimax looks good in opencode

---

continue