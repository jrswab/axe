# Session Context

## User Prompts

### Prompt 1

Run `git diff` and return the full output so I can review the changes. Then run `git diff --stat` to show a summary. Do NOT commit yet — just show me the diff.

